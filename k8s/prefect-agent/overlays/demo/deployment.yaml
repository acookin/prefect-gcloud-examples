# Autogenerated: do not edit

apiVersion: apps/v1
kind: Deployment
metadata:
  name: prefect-agent
  namespace: prefect
spec:
  template:
    spec:
      containers:
        - name: prefect-agent
          command:
            - prefect
            - agent
            - start
            
            - --work-queue
            - hello-all
            
            - --work-queue
            - hello-nope
            
            - --work-queue
            - child-flow
            
            - --work-queue
            - parent-flow
            
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: prefect-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: demo-prefect-agent
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: prefect-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: demo-prefect-agent