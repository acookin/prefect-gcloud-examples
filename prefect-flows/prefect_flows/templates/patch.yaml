# Autogenerated: do not edit

apiVersion: apps/v1
kind: Deployment
metadata:
  name: prefect-agent
  namespace: prefect
spec:
  template:
    spec:
      containers:
        - name: prefect-agent
          command:
            - prefect
            - agent
            - start
            {% for queue in work_queues %}
            - --work-queue
            - {{ queue }}
            {% endfor %}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: prefect-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ flow_group_name }}-prefect-agent
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: prefect-agent
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ flow_group_name }}-prefect-agent
